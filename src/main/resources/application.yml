#logging:
#  pattern:
#    console: "%d - %msg%n"
##  path: /var/log/tomcat
#  file: /var/log/tomcat/sell.log
#  level: debug

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/sell?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true
    username: root
    password: 123456
  jpa:
    show-sql: true
server:
  servlet:
    context-path: /sell
